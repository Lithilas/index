<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Capture The Point</title>
    <style>
        :root {
            --red-team: #e74c3c;
            --blue-team: #3498db;
            --bg-dark: #000000;
            --text-light: #ecf0f1;
            --btn-bg: #34495e;
            --accent: #f1c40f;
        }
        * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
        body { background-color: var(--bg-dark); color: var(--text-light); overflow-x: hidden; user-select: none; }
        
        .screen { display: none; min-height: 100vh; flex-direction: column; align-items: center; justify-content: center; padding: 20px; text-align: center; position: relative; }
        .active { display: flex; }
        
        h1 { margin-bottom: 20px; font-size: 2.5rem; }
        h2 { margin-bottom: 15px; font-size: 1.8rem; color: var(--accent); }
        
        .btn { background-color: var(--btn-bg); color: white; border: none; padding: 15px 30px; font-size: 1.2rem; border-radius: 8px; margin: 10px; cursor: pointer; transition: 0.2s; width: 80%; max-width: 300px; }
        .btn:active { transform: scale(0.95); }
        .btn-red { background-color: var(--red-team); }
        .btn-blue { background-color: var(--blue-team); }
        .btn-pause { background-color: #e67e22; position: fixed; top: 10px; right: 10px; width: auto; padding: 10px 20px; z-index: 100; display: none; }

        .timers { display: flex; flex-direction: column; gap: 15px; margin-bottom: 30px; width: 100%; max-width: 400px; background: rgba(255,255,255,0.1); padding: 20px; border-radius: 12px; }
        .timer-box { font-size: 2rem; font-weight: bold; }
        .timer-label { font-size: 1rem; color: #bdc3c7; text-transform: uppercase; letter-spacing: 1px; }
        .global-timer { color: var(--accent); }

        .status-indicator { font-size: 1.5rem; margin: 20px 0; padding: 15px; background: rgba(255,255,255,0.1); border-radius: 8px; width: 100%; max-width: 400px; }
        
        /* Pause Overlay */
        #paused-overlay { position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(0,0,0,0.9); z-index: 200; display: none; flex-direction: column; align-items: center; justify-content: center; }
        #paused-overlay h1 { color: #e67e22; }

        /* Hidden GM Button */
        .gm-secret-btn { position: absolute; bottom: 0; right: 0; width: 80px; height: 80px; background: transparent; color: rgba(255,255,255,0.02); font-size: 10px; display: flex; align-items: flex-end; justify-content: flex-end; padding: 5px; cursor: pointer; z-index: 50; }
    </style>
</head>
<body>

    <!-- Global Pause Overlay -->
    <div id="paused-overlay">
        <h1>GAME PAUSED</h1>
        <p style="font-size: 1.2rem;">Capture timer is frozen.</p>
        <p style="color: var(--accent); font-weight: bold; margin-top: 20px;">GLOBAL TIME IS STILL RUNNING:</p>
        <div class="timer-box global-timer" id="pause-global-time" style="margin-bottom: 30px; font-size: 3rem;">105:00</div>
        <!-- FIXED: Removed display:none so the resume button actually shows up -->
        <button class="btn btn-pause-toggle" style="background-color: #27ae60;" id="resume-btn">Resume Game</button>
    </div>

    <!-- Attacker Pause Button -->
    <button class="btn btn-pause btn-pause-toggle" id="top-pause-btn">Pause</button>

    <!-- 0. LOADING SCREEN -->
    <div id="screen-loading" class="screen active">
        <h1 id="loading-header">Connecting...</h1>
        <p id="loading-sub">Synchronizing with Firebase server</p>
    </div>

    <!-- 1. ROLE SELECTION -->
    <div id="screen-role" class="screen">
        <h1>Select Role</h1>
        <button class="btn" onclick="setRole('attacker')">ATTACKER</button>
        <button class="btn" onclick="setRole('defender')">DEFENDER (View Only)</button>
        
        <!-- HIDDEN GM BUTTON -->
        <div class="gm-secret-btn" onclick="setRole('gm')">GM</div>
    </div>

    <!-- 2. MAIN MENU (Attackers Only) -->
    <div id="screen-menu" class="screen">
        <h1>Game Setup</h1>
        <div style="margin-bottom: 20px;">
            <h3>Select Team</h3>
            <button class="btn btn-red" onclick="selectTeam('red')">RED TEAM</button>
            <button class="btn btn-blue" onclick="selectTeam('blue')">BLUE TEAM</button>
        </div>
        <div style="margin-bottom: 20px;">
            <h3>Select Mode</h3>
            <button class="btn" onclick="selectMode('normal')">NORMAL MODE</button>
            <button class="btn" onclick="selectMode('demo')">DEMO MODE</button>
        </div>
        <h2 id="setup-summary">Team: None | Mode: None</h2>
        <button class="btn" style="background-color: #27ae60;" onclick="startGame()">START GAME</button>
    </div>

    <!-- 3. TRAVEL PHASE (Pre-CP) -->
    <div id="screen-travel" class="screen">
        <h1 id="travel-title">Traveling...</h1>
        <div class="timers">
            <div class="timer-label">Global Time Remaining</div>
            <div class="timer-box global-timer" id="travel-global-time">105:00</div>
        </div>
        <p style="margin-bottom: 30px;">Physically move to the next Control Point.</p>
        <button class="btn" style="background-color: #27ae60;" onclick="arriveAtPoint()">WE HAVE ARRIVED</button>
    </div>

    <!-- 4. CAPTURE PHASE (CP) -->
    <div id="screen-capture" class="screen">
        <h1 id="capture-title">Capturing CP1</h1>
        <div class="timers">
            <div>
                <div class="timer-label">Global Time Remaining</div>
                <div class="timer-box global-timer" id="capture-global-time">105:00</div>
            </div>
            <div>
                <div class="timer-label">Capture Progress</div>
                <div class="timer-box" id="capture-local-time">20:00</div>
            </div>
        </div>
        
        <p style="color: #bdc3c7; margin-top: 10px;">Defend this point until the capture timer hits zero.</p>
    </div>

    <!-- 5. BASE CAPTURED INTERMISSION -->
    <div id="screen-base" class="screen">
        <h1>BASE CAPTURED!</h1>
        <div class="timers">
            <div class="timer-label">Global Time Remaining</div>
            <div class="timer-box global-timer" id="base-global-time">105:00</div>
        </div>
        <h2 style="color: #2ecc71; margin-top: 10px;">Your new base is Point 1.</h2>
        <p style="margin-bottom: 20px;">Prepare to travel to the final control point.</p>
        <button class="btn" style="background-color: #2980b9;" onclick="continueToBaseTravel()">CONTINUE</button>
    </div>

    <!-- 6. END SCREENS -->
    <div id="screen-end" class="screen">
        <h1 id="end-title" style="font-size: 4rem;">VICTORY</h1>
        <p id="end-subtitle">All points captured in time.</p>
        <button class="btn" onclick="resetApp()">Back to Menu</button>
    </div>

    <!-- 7. DEFENDER DASHBOARD -->
    <div id="screen-defender" class="screen">
        <h1>Defender Dashboard</h1>
        <h2 id="def-team-display">Waiting for Attackers...</h2>
        
        <div class="timers">
            <div>
                <div class="timer-label">Defense Global Time Limit</div>
                <div class="timer-box global-timer" id="def-global-time">105:00</div>
            </div>
            <div id="def-local-container" style="display: none;">
                <div class="timer-label" id="def-local-label">Current Capture Timer</div>
                <div class="timer-box" id="def-local-time">--:--</div>
            </div>
        </div>

        <div class="status-indicator" id="def-status">
            Current Phase: Waiting to Start
        </div>
    </div>

    <!-- 8. GAME MASTER DASHBOARD (HIDDEN) -->
    <div id="screen-gm" class="screen">
        <h1 style="color: #9b59b6;">Game Master Console</h1>
        <div class="status-indicator" id="gm-status" style="font-weight: bold;">
            Phase: Menu
        </div>
        
        <div class="timers" style="flex-direction: row; justify-content: center; max-width: 500px;">
            <div style="margin: 0 15px;">
                <div class="timer-label">Global Timer</div>
                <div class="timer-box global-timer" id="gm-global-time">105:00</div>
            </div>
            <div style="margin: 0 15px;">
                <div class="timer-label">Local Timer</div>
                <div class="timer-box" id="gm-local-time" style="color: #3498db;">--:--</div>
            </div>
        </div>

        <div style="background: rgba(255,255,255,0.1); padding: 15px; border-radius: 8px; width: 100%; max-width: 400px; margin-bottom: 20px;">
            <h3 style="margin-bottom: 10px;">Setup (Before Start)</h3>
            <button class="btn btn-red" style="padding: 10px 15px; font-size: 1rem; width: auto;" onclick="selectTeam('red')">RED</button>
            <button class="btn btn-blue" style="padding: 10px 15px; font-size: 1rem; width: auto;" onclick="selectTeam('blue')">BLUE</button><br>
            <button class="btn" style="padding: 10px 15px; font-size: 1rem; width: auto;" onclick="selectMode('normal')">NORMAL</button>
            <button class="btn" style="padding: 10px 15px; font-size: 1rem; width: auto;" onclick="selectMode('demo')">DEMO</button>
            <div id="gm-setup-summary" style="margin-top:10px; font-weight: bold; color: #f1c40f;">Setup: None / None</div>
        </div>

        <div style="display: flex; flex-wrap: wrap; justify-content: center; gap: 10px; max-width: 500px;">
            <button class="btn" style="background-color: #27ae60; width: 45%; margin: 5px;" onclick="gmStartGame()">FORCE START</button>
            <button class="btn btn-pause-toggle" style="background-color: #e67e22; width: 45%; margin: 5px; display: block;">TOGGLE PAUSE</button>
            <button class="btn" style="background-color: #8e44ad; width: 45%; margin: 5px;" onclick="gmNextPhase()">NEXT PHASE</button>
            <button class="btn" style="background-color: #c0392b; width: 45%; margin: 5px;" onclick="gmResetApp()">RESET GAME</button>
        </div>
    </div>

    <!-- Firebase SDK -->
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js";
        import { getAnalytics } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-analytics.js";
        import { getDatabase, ref, set, onValue } from "https://www.gstatic.com/firebasejs/10.8.0/firebase-database.js";

        // Firebase configuration
        const firebaseConfig = {
            apiKey: "AIzaSyAGhi4XMLeVGnRq3dhhwbJK9NOCtIPJ9vs",
            authDomain: "cprush-2249a.firebaseapp.com",
            databaseURL: "https://cprush-2249a-default-rtdb.europe-west1.firebasedatabase.app",
            projectId: "cprush-2249a",
            storageBucket: "cprush-2249a.firebasestorage.app",
            messagingSenderId: "40928992118",
            appId: "1:40928992118:web:86c09b9eab9d9539c67857",
            measurementId: "G-7N5M3W4ZYB"
        };

        const app = initializeApp(firebaseConfig);
        const analytics = getAnalytics(app);
        const db = getDatabase(app);
        const gameRef = ref(db, 'game');

        const TIMERS = {
            global: 105 * 60,
            normal: { cp1: 20 * 60, cp2: 18 * 60, cp3: 15 * 60 },
            demo: { cp1: 3 * 60, cp2: 2 * 60, cp3: 1 * 60 }
        };

        let role = null; // 'attacker', 'defender', or 'gm'
        let setupTeam = null;
        let setupMode = null;
        let masterTickInterval = null;
        let isInitialized = false;

        let state = {
            phase: 'menu', 
            globalTime: TIMERS.global,
            cpTime: 0,
            paused: false,
            mode: 'normal',
            team: 'red'
        };

        onValue(gameRef, (snapshot) => {
            const data = snapshot.val();
            
            if (data) {
                state = data;
            } else {
                set(gameRef, state).catch(err => console.error("Write error:", err));
            }

            if (!isInitialized) {
                isInitialized = true;
                document.getElementById('screen-loading').classList.remove('active');
                if (!role) {
                    document.getElementById('screen-role').classList.add('active');
                }
            }

            if (role) {
                if ((role === 'attacker' || role === 'gm') && state.phase !== 'menu' && state.phase !== 'victory' && state.phase !== 'gameover' && !masterTickInterval) {
                    startMasterTick();
                }
                if (state.phase === 'menu' && masterTickInterval) {
                    clearInterval(masterTickInterval);
                    masterTickInterval = null;
                }
                updateUI();
            }
        }, (error) => {
            document.getElementById('loading-header').innerText = "Firebase Error!";
            document.getElementById('loading-header').style.color = "#e74c3c";
            
            if (error.message.includes("Permission denied")) {
                document.getElementById('loading-sub').innerHTML = `<b>Permission Denied!</b><br>Check Firebase Rules.`;
            } else {
                document.getElementById('loading-sub').innerText = error.message;
            }
        });

        window.setRole = (selectedRole) => {
            role = selectedRole;
            if (role === 'attacker') {
                document.getElementById('top-pause-btn').style.display = 'block';
            } else {
                document.getElementById('top-pause-btn').style.display = 'none';
            }
            updateUI();
        };

        window.selectTeam = (team) => { setupTeam = team; updateSetupSummary(); };
        window.selectMode = (mode) => { setupMode = mode; updateSetupSummary(); };
        
        function updateSetupSummary() {
            document.getElementById('setup-summary').innerText = `Team: ${setupTeam || 'None'} | Mode: ${setupMode || 'None'}`;
            document.getElementById('gm-setup-summary').innerText = `Setup: ${setupTeam || 'None'} / ${setupMode || 'None'}`;
        }

        // --- Attacker game initialization ---
        window.startGame = () => {
            if (!setupTeam || !setupMode) return alert("Select Team and Mode first!");
            state = { phase: 'pre1', globalTime: TIMERS.global, cpTime: setupMode === 'normal' ? TIMERS.normal.cp1 : TIMERS.demo.cp1, paused: false, mode: setupMode, team: setupTeam };
            pushState();
            startMasterTick();
        };

        window.arriveAtPoint = () => {
            if (state.phase === 'pre1') state.phase = 'cp1';
            else if (state.phase === 'pre2') state.phase = 'cp2';
            else if (state.phase === 'pre3') state.phase = 'cp3';
            pushState();
        };

        window.continueToBaseTravel = () => {
            state.phase = 'pre3';
            state.cpTime = state.mode === 'normal' ? TIMERS.normal.cp3 : TIMERS.demo.cp3;
            pushState();
        };

        // --- Game Master exact initialization override ---
        window.gmStartGame = () => {
            if (state.phase !== 'menu' && !confirm("Game is already running! Force restart?")) return;
            if (!setupTeam || !setupMode) return alert("Select Team and Mode in Setup first!");
            
            state = { 
                phase: 'pre1', 
                globalTime: TIMERS.global, 
                cpTime: setupMode === 'normal' ? TIMERS.normal.cp1 : TIMERS.demo.cp1, 
                paused: false, 
                mode: setupMode, 
                team: setupTeam 
            };
            pushState();
            startMasterTick();
        };

        window.gmResetApp = () => {
            if (!confirm("Are you sure you want to completely RESET the global game state?")) return;
            const resetState = { phase: 'menu', globalTime: TIMERS.global, cpTime: 0, paused: false, mode: 'normal', team: 'red' };
            set(gameRef, resetState);
        };

        window.gmNextPhase = () => {
            if (state.phase === 'menu' || state.phase === 'victory' || state.phase === 'gameover') return alert("Start the game first.");
            
            if (state.phase === 'pre1') { state.phase = 'cp1'; }
            else if (state.phase === 'cp1') { state.phase = 'pre2'; state.cpTime = state.mode === 'normal' ? TIMERS.normal.cp2 : TIMERS.demo.cp2; }
            else if (state.phase === 'pre2') { state.phase = 'cp2'; }
            else if (state.phase === 'cp2') { state.phase = 'base'; }
            else if (state.phase === 'base') { state.phase = 'pre3'; state.cpTime = state.mode === 'normal' ? TIMERS.normal.cp3 : TIMERS.demo.cp3; }
            else if (state.phase === 'pre3') { state.phase = 'cp3'; }
            else if (state.phase === 'cp3') { state.phase = 'victory'; }
            
            pushState();
        }

        window.resetApp = () => {
            if (role === 'attacker') {
                const resetState = { phase: 'menu', globalTime: TIMERS.global, cpTime: 0, paused: false, mode: 'normal', team: 'red' };
                set(gameRef, resetState).then(() => location.reload());
            } else {
                location.reload();
            }
        };

        // --- Pause Mechanic ---
        const togglePause = () => {
            if (role !== 'attacker' && role !== 'gm') return;
            state.paused = !state.paused;
            pushState();
        };
        document.querySelectorAll('.btn-pause-toggle').forEach(btn => btn.addEventListener('click', togglePause));

        // --- Master Game Loop ---
        function startMasterTick() {
            if (masterTickInterval) clearInterval(masterTickInterval);
            
            masterTickInterval = setInterval(() => {
                if (state.phase === 'menu' || state.phase === 'victory' || state.phase === 'gameover') return;

                state.globalTime--;
                
                if (state.globalTime <= 0) {
                    state.globalTime = 0;
                    state.phase = 'gameover';
                    pushState();
                    clearInterval(masterTickInterval);
                    return;
                }

                if (!state.paused && state.phase.startsWith('cp')) {
                    state.cpTime -= 1;
                    if (state.cpTime <= 0) {
                        state.cpTime = 0;
                        handleCaptureComplete();
                    }
                }
                
                pushState();
            }, 1000);
        }

        function handleCaptureComplete() {
            if (state.phase === 'cp1') {
                state.phase = 'pre2';
                state.cpTime = state.mode === 'normal' ? TIMERS.normal.cp2 : TIMERS.demo.cp2;
            } else if (state.phase === 'cp2') {
                state.phase = 'base';
            } else if (state.phase === 'cp3') {
                state.phase = 'victory';
            }
        }

        function pushState() {
            if (role === 'attacker' || role === 'gm') {
                set(gameRef, state).catch(err => console.error(err));
                updateUI(); 
            }
        }

        // --- UI Rendering ---
        function updateUI() {
            const overlay = document.getElementById('paused-overlay');
            overlay.style.display = (state.paused && role !== 'gm') ? 'flex' : 'none';

            const formattedGlobal = formatTime(state.globalTime);
            const formattedLocal = formatTime(state.cpTime);
            
            document.getElementById('pause-global-time').innerText = formattedGlobal;
            document.querySelectorAll('.screen').forEach(el => el.classList.remove('active'));

            if (role === 'attacker') {
                if (state.phase === 'menu') {
                    document.getElementById('screen-menu').classList.add('active');
                } else if (state.phase.startsWith('pre')) {
                    document.getElementById('screen-travel').classList.add('active');
                    const num = state.phase.replace('pre', '');
                    document.getElementById('travel-title').innerText = `Traveling to CP${num}`;
                    document.getElementById('travel-global-time').innerText = formattedGlobal;
                } else if (state.phase.startsWith('cp')) {
                    document.getElementById('screen-capture').classList.add('active');
                    document.getElementById('capture-title').innerText = `Capturing CP${state.phase.replace('cp', '')}`;
                    document.getElementById('capture-global-time').innerText = formattedGlobal;
                    document.getElementById('capture-local-time').innerText = formattedLocal;
                } else if (state.phase === 'base') {
                    document.getElementById('screen-base').classList.add('active');
                    document.getElementById('base-global-time').innerText = formattedGlobal;
                } else if (state.phase === 'victory' || state.phase === 'gameover') {
                    document.getElementById('screen-end').classList.add('active');
                    const endTitle = document.getElementById('end-title');
                    const endSub = document.getElementById('end-subtitle');
                    if (state.phase === 'victory') {
                        endTitle.innerText = "VICTORY";
                        endTitle.style.color = "#2ecc71";
                        endSub.innerText = `The ${state.team} team secured all points!`;
                    } else {
                        endTitle.innerText = "GAME OVER";
                        endTitle.style.color = "#e74c3c";
                        endSub.innerText = "The Defenders successfully held the territory. Time ran out.";
                    }
                }
            } else if (role === 'defender') {
                document.getElementById('screen-defender').classList.add('active');
                document.getElementById('def-team-display').innerText = state.team === 'red' ? '🔴 RED TEAM ATTACKING' : '🔵 BLUE TEAM ATTACKING';
                document.getElementById('def-global-time').innerText = formattedGlobal;
                
                const statusDiv = document.getElementById('def-status');
                const localContainer = document.getElementById('def-local-container');

                const phaseMap = { menu: "Waiting for Attackers to Start...", pre1: "Attackers traveling to CP1", cp1: "Defending CP1!", pre2: "Attackers traveling to CP2", cp2: "Defending CP2!", base: "Base Captured by Attackers - Waiting to travel", pre3: "Attackers traveling to Final CP", cp3: "Defending Final CP!", victory: "DEFEAT - Attackers captured all points.", gameover: "VICTORY - You successfully defended the territory!" };

                let statusText = `Phase: ${phaseMap[state.phase] || ''}`;
                if (state.paused && state.phase !== 'menu' && state.phase !== 'victory' && state.phase !== 'gameover') {
                    statusText += " (CAPTURE PAUSED)";
                }
                statusDiv.innerText = statusText;

                if (state.phase.startsWith('cp')) {
                    localContainer.style.display = 'block';
                    document.getElementById('def-local-time').innerText = formattedLocal;
                    statusDiv.style.backgroundColor = state.paused ? '#e67e22' : 'var(--red-team)';
                } else {
                    localContainer.style.display = 'none';
                    statusDiv.style.backgroundColor = 'rgba(255,255,255,0.1)';
                }
            } else if (role === 'gm') {
                document.getElementById('screen-gm').classList.add('active');
                document.getElementById('gm-global-time').innerText = formattedGlobal;
                document.getElementById('gm-local-time').innerText = state.phase.startsWith('cp') ? formattedLocal : '--:--';
                
                let phaseText = state.phase.toUpperCase();
                if (state.paused) phaseText += " (PAUSED)";
                document.getElementById('gm-status').innerText = `Phase: ${phaseText}`;
                
                if (state.paused) document.getElementById('gm-status').style.backgroundColor = '#e67e22';
                else document.getElementById('gm-status').style.backgroundColor = 'rgba(255,255,255,0.1)';
            }
        }

        function formatTime(totalSeconds) {
            if(totalSeconds < 0) totalSeconds = 0;
            const m = Math.floor(totalSeconds / 60).toString().padStart(2, '0');
            const s = (totalSeconds % 60).toString().padStart(2, '0');
            return `${m}:${s}`;
        }
    </script>
</body>
</html>
